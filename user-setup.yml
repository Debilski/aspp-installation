---
  - name: User setup
    remote_user: root
    become_user: student
    become: yes
    hosts: all
    tasks:
      - name: Install pip packages
        tags: packages
        pip:
          executable: pip3
          state: latest
          name:
            - ipython_memwatcher
            - pyprof2calltree
            - git+https://github.com/ASPP/prof3to2.git
            - numba
            - colorama >= 0.3.9 # numba requires a newer version than ubuntu provides
            - pandas >= 0.25
            - dask-jobqueue
            - pudb
            - pysnooper
            - altair
            - networkx >= 2.3
            - jupyter
            - jupyter-console < 6
            - ipyparallel
            - ipython >=6, <7
            - snakeviz
            - bokeh
            - prompt-toolkit<2.0.0,>=1.0.15 # ipython bug:
                                            # https://stackoverflow.com/questions/51676835/ipython-cannot-import-name-create-prompt-application-from-prompt-toolkit-s
            - vega_datasets
            - vega

      - name: Install Rike's vim config
        git:
          repo: 'https://github.com/Debilski/vim-basic.git'
          dest: /home/student/.vim
          clone: yes
          update: yes
        tags: remote

      - name: Enable Rike's vim config
        file:
          src: .vim/vimrc
          dest: /home/student/.vimrc
          state: link

      - name: Install vim plugins
        command: vim +PlugInstall +qall

      - name: Set up git
        copy:
          content: |
            [core]
                editor = atom -w
            [user]
                name = ASPP Student
                email = student@localhost

          dest: /home/student/.gitconfig

      - name: Disable screensaver
        command: gsettings set org.gnome.desktop.session idle-delay 0
      - name: Disable screen lock
        command: gsettings set org.gnome.desktop.lockdown disable-lock-screen true

      - name: Make python python3
        file:
          src: '/usr/bin/{{ item.src }}'
          dest: '/home/student/.local/bin/{{ item.dest }}'
          state: link
        with_items:
          - { src: python3,    dest: python }
          - { src: pdb3,       dest: pdb }
          - { src: pydoc3,     dest: pydoc }
          - { src: pygettext3, dest: pygettext }
          - { src: pip3,       dest: pip }
          - { src: pytest-3,   dest: pytest }

      - name: Install .bashrc
        copy:
          src: 'files/.bashrc'
          dest: '/home/student/.bashrc'

      - name: Remove .bash_history
        file:
          path: '/home/student/.bash_history'
          state: absent
        tags: cleanup

      - name: Remove .ipynb_checkpoints/
        file:
          path: '/home/student/.ipynb_checkpoints/'
          state: absent
        tags: cleanup

      - name: Set input keyboards to us/it/de
        dconf:
          key: /org/gnome/desktop/input-sources/sources
          value: "[('xkb', 'us'), ('xkb', 'it'), ('xkb', 'de')]"
          state: present
        tags: remote

      - name: Disable lock screen
        dconf:
          key: /org/gnome/desktop/lockdown/disable-lock-screen
          value: "true"
          state: present
        tags: remote

      - name: Setup favourites
        dconf:
          key: /org/gnome/shell/favorite-apps
          value: "['firefox.desktop', 'org.gnome.Terminal.desktop', 'org.gnome.Nautilus.desktop', 'atom.desktop', 'code.desktop', 'libreoffice-writer.desktop']"
          state: present
        tags: remote

