---
  - name: Disk setup
    remote_user: root
    hosts: all
    tasks:
      - name: Extend root to 200GB
        tags: remote
        script: files/resize-root.sh
        args:
          creates: /etc/.root-resized

      - name: Add nm profile
        tags: remote
        copy:
          src: files/UBx-invites.nmconnection
          dest: /etc/NetworkManager/system-connections/
        tags: remote

      - name: Add nm profile
        tags: remote
        copy:
          src: files/aspp2.nmconnection
          dest: /etc/NetworkManager/system-connections/
        tags: remote

      - name: Add nm profile
        tags: remote
        command: nmcli conn reload
